# User Preferences

- **GitHub Tooling**: Prefer using `gh` (GitHub CLI) for checking PR status, logs, and other GitHub interactions instead of the browser tool, to avoid rate limits and improve reliability.
